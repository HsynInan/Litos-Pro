/*
class Ayarlarim extends StatelessWidget {
  const Ayarlarim({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Theme.of(context).accentColor,
        title: Text("Ayarlar"),
      ),
      body: Column(
        mainAxisAlignment: MainAxisAlignment.center,
        crossAxisAlignment: CrossAxisAlignment.center,
        children: [
          Center(
              child: Text(
            "Yakın zamanda güncellenecektir.",
            style: TextStyle(fontSize: 24),
          ))
        ],
      ),
    );
  }
}
*/
